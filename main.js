//Function for Discount Button
function buttonDiscount () {
    let Budge = document.getElementById(budget).value;
    let discval = Number(Budge * 0.2)

    document.getElemetnById().InnerHTML = 
        <div class="container"></div>
}